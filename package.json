{
  "name": "@pareo/tokens",
  "version": "0.2.0",
  "description": "The single source of truth for the Pareo design system — brand colors, type, radii and theming, framework-agnostic.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pareo-AI/tokens.git"
  },
  "type": "module",
  "sideEffects": [
    "*.css"
  ],
  "exports": {
    "./theme.css": "./theme.css",
    "./package.json": "./package.json"
  },
  "files": [
    "theme.css"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7"
  }
}
